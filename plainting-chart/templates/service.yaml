apiVersion: v1
kind: Service
metadata:
  name: {{ include "plainting-chart.fullname" . }}
  labels:
    {{- include "plainting-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plainting-chart.selectorLabels" . | nindent 4 }}
